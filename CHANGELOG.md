# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added
- 新增本地 SQLite 历史数据库，保存合同、风险问题、履约台账、聊天和法律依据复核结果；首次连接时自动迁移旧的浏览器历史。
- 新增本地向量检索服务：使用量化 `bge-small-zh-v1.5` 为 2,533 条法条建立 512 维索引，严格一条法条对应一个存储向量，并通过 `/api/legal-search` 与本地关键词结果混合召回。
- 新增向量服务状态提示、一键 Windows 启动脚本与法条向量索引重建脚本。
- 常见合同风险检索新增概念锨点，覆盖主体资质与授权、通知送达、单方变更、合同期限及不可抗力等法规中表述不一致的场景。
- 法律依据召回升级为分层检索：合同类型与风险主题路由、标题／结构／标签／正文加权、窄适用法规触发门槛、跨来源候选控制及 LLM 直接／补充依据复核；长合同改为首中尾抽样。
- 法律结构解析新增司法解释常用的“一、……”与“（一）……”分组层级，避免无“章／节”文件的标题信息丢失。
- 法律数据生成流程新增官方网页页脚噪声清理和“编／章／节”层级解析；保留法条自然段（款）边界，并在法律依据库中显示结构路径。
- 修复《劳动法》PDF版面软换行造成的中文断词，并在法律依据库正文中移除已经单独显示的重复条号。
- 护眼主题进一步调整为深墨绿工作台与暖米色纸张的高层次组合，去除低对比灰绿色造成的“灰蒙”观感。
- 主界面采用低亮度暖灰纸张配色，减少长时间合同阅读时的大面积白屏刺激；页面初始化、审核完成和历史记录载入后均默认优先显示“合同对照”。
- 扩展现行法律依据库：在《民法典》之外接入24份法律及司法解释、1,273条结构化条文，并保留官方来源、版本和生效日期。
- 新增统一法律依据库页面，支持按文件浏览、跨库关键词检索、AI咨询和官方来源跳转。
- 合同对照支持双向段落联动：点击任一侧正文，另一侧自动滚动并高亮对应段落；风险定位同时联动两栏。
- 审核重点支持“合同类型 × 审核立场”场景化联动，并按组合保存用户自定义内容。
- 结构化风险审查结果：风险等级、分类、影响方、置信度、理由和可替换条款
- 合同类型与审查立场选择
- 风险等级筛选、原文定位、忽略以及逐条采纳修改
- 审查核心解析与页面交互自动化测试
- 合同核心要素与履约事项台账，支持 CSV 导出、明确日期的 ICS 日历导出和履约状态记录

### Changed
- 全局按钮重构为主操作、普通操作、轻量操作和危险操作四级体系；统一紧凑尺寸、交互反馈与键盘焦点，并为标签页、风险筛选、浮动入口和关闭按钮保留各自合适的控件形态。
- 历史记录的“重新核对”改为紧凑的低饱和文字型操作按钮，避免继承全局主按钮的大尺寸和高对比样式。
- 历史记录现在直接恢复已保存的向量候选、AI采用法条、依据类型、复核理由、时间、模型和法律库版本，不再在每次加载时重复调用AI；旧记录可通过明示按钮手动重新核对。
- AI 法条复核状态拆分为“AI已复核并采用”、“AI已复核但未采用候选”和“AI复核失败”；无效 JSON 不再被静默当作空结果，界面会显示 HTTP 或解析错误原因。
- 法律检索查询不再使用修改建议文本，避免“支付、违约”等拟稿用语污染当前风险主题；向量结果仍受合同类型、主题路由和窄适用法规触发条件约束。
- 法律依据改为按标注元素的 `data-issue-index` 绑定原问题，修复部分问题无法定位时后续法条整体错位的问题。
- AI 法律依据复核返回空结果或请求异常时，保留最多 3 条高置信候选，并明确标注为“本地初筛·待核对”，不再将有效候选整体隐藏。
- 主界面和法律依据库完成专业法律工作台视觉重构：采用低饱和墨绿、深灰与暖白配色，统一表单、按钮、卡片、标签、弹窗及双栏对照层级，并减少装饰性图标。
- 法律依据库目录与选择器将“法律”和“司法解释、规定及批复”分栏展示，不再混排。
- 每条法条及审核引用在开头显示修订版本或司法解释文号、施行日期和有效期截止日（如适用）。
- 审核、问题依据核对、段落优化和合同问答统一改用现行法律依据候选检索；引用使用“文件全称＋条号＋唯一ID”避免同号法条混淆。
- 审查立场改为动态执行规则；中立审查明确要求分别识别双方风险、标注受影响方并评价权责是否对等。
- 将内联样式和主业务脚本从 `index.html` 分离至 `css/app.css` 与 `js/app.js`。
- 标注合同与编辑合同合并为“合同对照”标签，并恢复桌面端左右双栏对比布局。
- 审核结果区改为标签式工作台；风险清单、合同要素与履约、标注合同、编辑合同可直接切换，长内容在面板内滚动。
- DeepSeek 模型更新为 `deepseek-v4-flash` 和 `deepseek-v4-pro`
- 审查接口改用 JSON Output，页面自行安全渲染标注内容

### Fixed
- 修复长合同联动时滚动了编辑器外层容器、导致目标段落仍停留在可视区外的问题；现在会识别最近的真实滚动容器。
- 历史记录加载时不再重复创建记录
- 修复 Quill 历史编辑内容恢复方法名
- 关闭 DeepSeek 审查请求的默认思考模式，并在空响应、代码块或无效 JSON 时自动清理和重试一次
- 审查输出上限调整为首轮 64K；若因长度截断，修复重试自动提升至 128K
- 段落优化改用关闭思考模式的 JSON Output，并在空内容或格式异常时自动重试

## [1.2.0] - 2026-06-15

### Added
- 民法典查询页，支持全文浏览、关键词/条号搜索、AI 咨询和快速定位条文
- 合同审核意见下方展示 AI 核对后的相关民法典依据
- AI 段落优化和合同问答支持参考相关民法典条文
- 工具顶部与民法典查询页增加免责声明

### Changed
- README 图片统一移动到 `pics/` 目录

## [1.1.1] - 2026-06-13

### Added
- AI 优化原文：右侧原文段落悬停显示 AI 优化按钮，结合审核建议生成可编辑优化版本，确认后替换原文

## [1.1.0] - 2026-05-28

### Added
- 右侧原文面板集成 Quill 富文本编辑器，支持在线修改合同内容
- "下载修改后合同"按钮，编辑内容写入原 DOCX 并保留原始格式
- 本地引入 Quill.js 及样式文件

### Changed
- 上传合同后右侧面板从只读预览改为可编辑的富文本编辑器
- 工具栏支持加粗、颜色、标题、列表等格式操作

## [1.0.0] - 2026-05-22

### Added
- 初始版本 | Initial release
- DOCX 上传与解析
- DeepSeek AI 智能审核（风险提示、条款建议）
- 合同内容自由问答
- 隐私优先设计

